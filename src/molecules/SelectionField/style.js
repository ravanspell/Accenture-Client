import { makeStyles } from "@material-ui/core";

const styles = makeStyles({
    labelContainer: {
        margin: '8px 0px'
    },
    errorContainer: {
        margin: '5px 0px'
    },
    errorText: {
        color: 'red',
        fontSize: '14px',
        fontWeight: 400
    }
})

export default styles;